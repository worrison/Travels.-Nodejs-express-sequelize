{{#if error}}
    <div class="alert alert-danger" role="alert">
        ERROR: {{error}}
    </div>
{{/if}}
<section class="container-flex">
{{!-- <table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Destino</th>
            <th scope="col">Salida</th>
            <th scope="col">Llegada</th>
            <th scope="col">Precio</th>
            <th scope="col">Descuento</th>
            <th scope="col">Usuario</th>
            <th scope="col"></th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        {{#each travels}}
        <tr>
            <td>{{destino}}</td>
            <td>{{fecha_salida}}</td>
            <td>{{fecha_llegada}}</td>
            <td>{{precio}}</td>
            <td>{{descuento}}</td>
            <td>{{user.name}}</td>
            <td><a href="">edit</a></td>
            <td><a href="/travels/delete/{{id}}">X</a></td>
        </tr>
        {{/each}}
    </tbody>
</table> --}}

<div class="d-flex flex-wrap justify-content-center">
{{#each travels}}

  <div class="card col-3">
    <a href="/travels/detail/{{id}}">
      <img src="../../upload/5slider.jpg" class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title">{{destino}}</h5>
        <p class="card-text">{{fecha_salida}}-{{fecha_llegada}}</p>
      </div>
      <div class="card-footer">
        <small class="text-muted">{{precio}}</small>
        {{#if ../rolUser}}<small class="text-muted">Creado por:{{user.name}}</small>{{/if}}
      </div>
    </a>
  </div>

{{/each}}

</div>



     {{#if rolUser}}<a href="/travels/add" class="btn btn-primary">Add Travel</a>{{/if}}
</section>